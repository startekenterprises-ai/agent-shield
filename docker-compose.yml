version: '3.8'

services:
  agent-shield:
    build: .
    container_name: agent-shield-gateway
    ports:
      - "8000:8000"
    # Natively injects variables declared inside your local .env file
    env_file:
      - .env
    restart: unless-stopped

